stages:
1. untracked
2. modified
3. staged
4. unmodified


git config --global user.name "your name"
git config --global user.email "example@gmail.com"
git config --list

clear

git --version

git clone <-link->

git status
git ls
git ls -a

cd <-directory name->
cd ..

git add <-file name-> or "file name"
git add . //works in the current folder
git add -A //add everything inside the repo


git commit -m "some message'

git rm <-file name-> or "file name"

touch <-file name->

git push
git push -u origin main //setting upstream

mkdir <-reponame-> //if single word filename
mkdir "filename" //else use this

git init

git remote add <-origin name-> <-link->
git remote -v

git branch //checking which branch im in rightnow
git branch -M <-name->//rename a branch
git checkout -b <-branch name->//creating new branch
git checkout <-branch name->//navigate between branches
git branch -d <-branch name->//delete branch [note: cannot delete the branch im currently in ]
git push origin <-branch-> //push the changes you made in the branch to github
git diff <-branch name->


git pull origin main //fetches the remotely made changes(the changes are merged usign pull request but it only happens on the remote file, so to brinng that change to local file's main, this comand is used)
ANUZ GHIMIRE


1. git merge <-branchname->//write branchname of the branch where you have made changes
2. git commit 
3. esc key press
4. type=>  :wq
5. press enter



//you created a branch and want to merge the changes
there are two ways:
1. using pull request in github
2. using git merge command






1. using pull reuest in github
- after merging remotely
- git pull origin main


2. using git merge command
- make changes in branch
- add the change
-  commit the change
- push the change
- use git merge command
- push the merge



//handling merge conflicts




//undoing changes
case1: staged changes(add)
git reset <-filename->// for one file 
git reset //for all files

restoring the previous file(reversing the changes and bring the file to its previous state)


case2: commited changes(for one commit)
git reset HEAD~1 //uncommits the last file which was committed
